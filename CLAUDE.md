@AGENTS.md

<!--
Working conventions live in AGENTS.md, shared by every coding agent. Claude Code reads CLAUDE.md
and not AGENTS.md, so this file imports it with the documented `@path` syntax instead of holding a
second copy: https://code.claude.com/docs/en/memory#agents-md
(A symlink is the alternative, but it needs Administrator or Developer Mode on Windows.)
This block is an HTML comment, which Claude Code strips before injecting the file into context, so
the note costs no tokens: https://code.claude.com/docs/en/memory#how-claude-md-files-load
-->
